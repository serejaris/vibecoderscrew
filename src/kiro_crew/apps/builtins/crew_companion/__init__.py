# Crew Companion builtin app — dashboard surface for the macOS desktop
# companion. The companion itself is a separate client app installed on the
# user's machine; this builtin contributes its dashboard page and proxies the
# dashboard UI to the companion's local backend on 127.0.0.1:7778.
